# PriceFM Stages R46--R48 frozen confirmation

R46 formalizes the completed R45 validation evidence. Both `NO_3` folds improve all seven quantiles under their case-specific R44 weights, pass quantile and horizon harm guards, reduce crossing, and preserve the median gain. Effective convergence includes every shared fit and only separate components with nonzero frozen weight; unused failures remain in the audit ledger.

R47 is a deterministic replay whose only model-contract change is adding the test split. It is not a new search. R48 must verify identical train/validation rows, feature maps, and validation predictions before test metrics are admissible. The frozen candidates are then compared against the current authoritative Q-DESN registry and cached PriceFM. Only candidates beating both references without test harm enter a later MCMC capability audit.

Registry mutation, article changes, and MCMC launch remain blocked throughout R46--R48.
